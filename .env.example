# Default: Instant zero-config SQLite database
DATABASE_URL=sqlite:///./hive.db

# PostgreSQL (Use when running Docker container via `docker-compose up -d`):
# DATABASE_URL=postgresql://hive_user:hive_password@localhost:5432/hive_db
